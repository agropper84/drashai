'use client';
// Screen 8: Library — all outputs across projects. Placeholder for Phase 5.

export default function LibraryPage() {
  return (
    <div>
      <div className="sy-eyebrow" style={{ marginBottom: 8 }}>Library</div>
      <div className="sy-page-title" style={{ marginBottom: 24 }}>Everything you've made</div>
      <div className="sy-empty-card">
        Your generated audio files, transcripts, notes, and questions will appear here.
      </div>
    </div>
  );
}
